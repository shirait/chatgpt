<%= form_with(model: @tag, url: tag_path(@tag), method: :put, local: true) do |f| %>
  <%= render("form", f: f) %>
  <%= f.submit(t("common.update"), class: "button is-primary is-normal mt-1") %>
<% end %>

<%= link_to t("common.back"), tags_path, class: "button is-warning is-normal mt-1" %>