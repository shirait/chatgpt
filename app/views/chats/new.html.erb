<%= form_with model: @user_message, url: chats_path, method: :post do |f| %>
  <%= f.text_area(:content, size: "80x5", placeholder: '質問を入力してください') %>
  <div>
    <%= f.submit('質問する') %>
  </div>
<% end %>

